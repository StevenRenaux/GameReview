{{ form_start(form) }}

    <div>{{form_label (form.name) }}</div>
    <div class="edit-form-input">{{form_widget (form.name) }}</div>

    <button type="submit" class="btn btn-warning">{{ button_label|default('Ajouter') }}</button>

{{ form_end(form)  }}