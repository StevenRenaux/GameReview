{{ form_start(form) }}

    <div>{{form_label (form.title) }}</div>
    <div>{{form_widget (form.title) }}</div>
    <div class="comment-form-help">{{form_help (form.title) }}</div>

    <div>{{form_label (form.content) }}</div>
    <div class="edit-form-input">{{form_widget (form.content) }}</div>

    <button type="submit" class="btn btn-warning">{{ button_label|default('Ajouter') }}</button>

{{ form_end(form)  }}